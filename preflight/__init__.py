"""Preflight: what has to be true before the stack is started, checked before starting it.

A runbook step is a hope. This package is the same knowledge as a program, so ``make up`` can
refuse rather than produce a stack that comes up healthy and wrong -- the shape every defect in
this repository's first month took. It is deliberately importable without a container runtime:
the classification and the comparison are pure, and only one check shells out.
"""
